BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[chambres] ADD [tarifJournalier] DECIMAL(18,2);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
