BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[patients] ADD [dateNaissance] DATE,
[numeroCmu] NVARCHAR(1000),
[numeroCni] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
